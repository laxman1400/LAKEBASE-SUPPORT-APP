# Local development only. Copy to .env and never commit the real file.
LAKEBASE_URL=postgresql://role:password@host/databricks_postgres?sslmode=require
MASSIVE_API_KEY=replace-me
MASSIVE_API_BASE_URL=https://api.massive.com
SUPPORT_SCHEMA=support_app
